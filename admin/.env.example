# Relative so the admin session cookie stays first-party; next.config rewrites
# /api/v1/* to API_ORIGIN.
NEXT_PUBLIC_API_URL=/api/v1

# Where the rewrite reaches the API. Not exposed to the browser.
API_ORIGIN=http://localhost:5000
