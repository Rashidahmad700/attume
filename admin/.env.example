# local | qa | production — drives the environment ribbon and nothing else
NEXT_PUBLIC_APP_ENV=local

# Admin panel talks to the same API, but only to /admin routes
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
